<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Editar información de cliente</title>
    <!-- Agrega el enlace a Bootstrap -->
    <link
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="container mt-5">
    <h1 class="mb-4">Editar cliente</h1>

    <!-- Formulario utilizando clases de Bootstrap -->
    <form
      method="POST"
      action="/actualizarCliente/<%= cliente.cliente_id %>"
      id="formupdate"
    >
      <div class="form-group">
        <!-- Campo de ID desactivado -->
        <label for="nombre">ID Cliente:</label>
        <input
          type="text"
          class="form-control"
          name="id"
          value="<%= cliente.cliente_id %>"
          disabled
        />
      </div>

      <div class="form-group">
        <label for="nombre">Nombre del cliente:</label>
        <input
          type="text"
          class="form-control"
          name="nombre"
          value="<%= cliente.nombre %> <%= cliente.apellido %>"
          disabled
        />
      </div>

      <div class="form-group">
        <label for="usuario">Usario:</label>
        <input
          type="text"
          class="form-control"
          name="usuario"
          value="<%= cliente.usuario %>"
          disabled
        />
      </div>

      <div class="form-group">
        <label for="correo">Correo:</label>
        <input
          type="text"
          class="form-control"
          name="correo"
          value="<%= cliente.correo %>"
        />
      </div>

      <div class="form-group">
        <label for="telefono">Teléfono:</label>
        <input
          type="text"
          class="form-control"
          name="telefono"
          value="<%= cliente.numerodetelefono %>"
        />
      </div>

      <div class="form-group">
        <label for="direccion">Dirección:</label>
        <input
          type="text"
          class="form-control"
          name="direccion"
          value="<%= cliente.direccion.calle %>"
        />
      </div>

      <!-- Agrega otros campos del cliente según sea necesario -->

      <button type="submit" class="btn btn-primary">Actualizar cliente</button>
      <button type="submit" class="btn btn-primary">Eliminar cliente</button>
    </form>

    <div class="alert alert-success mt-4" role="alert" id="mensajeExito"></div>
    <div class="alert alert-danger mt-4" role="alert" id="mensajeError"></div>

    <!-- Agrega el script al final del cuerpo del documento -->
    <script src="/static/edicionCliente/updateFetch.js"></script>

    <!-- Agrega el script de Bootstrap -->
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
  </body>
</html>
